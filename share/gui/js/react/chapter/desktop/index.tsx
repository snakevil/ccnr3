import * as React from 'react';

import * as Model from '../../../model';

export default function (props: { model: Model.IChapter, onClick: (model: Model.Bookshelf | Model.INovel | Model.IChapter) => void }) {
    return (
        <p>TODO: react.chapter.desktop</p>
    );
}
